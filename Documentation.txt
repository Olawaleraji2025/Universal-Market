1. I Linked the pages together using react router, I noticed that when i moved from the bottom of a page to another page, it doesnt start from the top i.e view. So i added a functionality that will move it to the tp everytime i switch pages.
I learnt about useNavigation, useLocation
I also learn to keep all components into a single file and export it as one, instead of making the App.js messy


2. I learn about Fetching data from supabase using Tanstack query. I learnt some keywords such as useQuery, useMutation e.t.c

3. I use Redux for statemanagement, when the user click view details, it collects the clicked product data and renders it in the ProductDetails.jsx. I also updated the Supabase database by adding a new column called ProductSpecification.

4. I installed React-Hook-form and change the Request form Ui using shadcn Field.

5. I learnt how to switch between pages using conditional rendering and Redux as state management. But i noticed that at my product page, if i refresh it, the product item will be lost and blank...... still going to work on it.

6. I Added signup functionality and success page; enhance flow management with new Redux actions

7. I Implement guest form validation with Zod; enhance form handling and state management in GuestForm and RequestModal components

8. I Enhance the GuestForm and RequestModal components; implement form submission with Supabase, add request date and ID, and improve validation messages I added Request Date, Item Status, Request ID, Contact.

9. I recheck my project and try t revised what i had implemented, the tools i used, learn more about them and how they improve this project.

10. I converted my Hero image to Webp.

11. I noticed that when i refresh the product page, the data is lost in redux.Learnt that redux stores data in ram memory. i had to use fetching the data from Url for persistency.

12. I want to implented the feature whereby when the use clicked on one of the category cards, it take the user to the shop page with the filtered category - i learnt how to implement that using useSearchparams(). But a bug occurred that after getting the data from the url,the user is unable to search and filter using input text and buttons.

13. I implementing a function that override the url param id when filtering and searching for items.... more details on it soon.

14. i Implemented the Skeleton feature to keep the user busy when the data is fetched and also make sure that the images load completely not in chunks before rendering